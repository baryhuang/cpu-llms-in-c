# RK3588 mainline Rocket NPU operating point used by the LLMC measurements.
# The driver parks the shared compute clock at 200 MHz while all cores are idle.
options rocket rocket_npu_clk_hz=600000000 rocket_autosuspend_ms=1000
